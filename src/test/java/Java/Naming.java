package Java;

public class Naming {

    public static void main(String[] args) {
        /*
        Project :: small letter
        Package :: capital
        Class :: Capital + Noun (Dog, Account)
        Interface :: Capital + Adjective (Runnable, ActionList, Sizeable )
        Method :: Small + Verb (getResult, DoCalculation)
        Variable :: Small + Small meaning word (doCalculation, getResults)
        Constructor ::
        Constant :: All letters Capital and to connect then connection character _ should be used (EXIT_ON_CLOSE, MIN_HEIGHT, PI)

        Note :: Avoid using special character in method and variable name.
         */
    }

    // ignore below 4 lines will be used in methods class
    public int p2;
    private int pvt2;
    int d2;
    protected int pro2;
}
